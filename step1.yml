---
- name: Step 1 - Preparation
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Create temporary directory
      file:
        path: ~/awx-workflow
        state: directory
    
    - name: Wait 30 seconds to observe logs
      pause:
        seconds: 30
    
    - name: Display step 1 message
      debug:
        msg: "Step 1 completed successfully!"
